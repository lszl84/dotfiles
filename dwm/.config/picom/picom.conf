# picom.conf — Compositor for dwm

backend = "glx";
vsync = true;

# No shadows
shadow = false;

# Dim inactive windows
inactive-dim = 0.35;

focus-exclude = [
    "window_type = 'dock'",
    "override_redirect = true"
];
