#!/bin/bash
# Toggle screen recording with wf-recorder

PID=$(pgrep -x wf-recorder)

if [ -n "$PID" ]; then
    # Stop recording
    kill "$PID"
else
    # Start recording
    mkdir -p ~/Videos
    wf-recorder -c libx264 -g "$(slurp)" -f ~/Videos/recording_$(date +%Y%m%d_%H%M%S).mp4 &
fi
